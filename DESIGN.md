# 设计说明

## 目标与素材

角色有普通和铁锅两种形态。原参考是有编号和色卡的拼豆图，每个格子对应一个像素。制作时分别提取成 48×62 与 41×50 像素的透明角色图，采用最近邻放大，保持拼豆画的边缘与色块。铁锅形态清理了图外的浅白背景格，并给外轮廓补上细深色边。最初的两张参考图片未放入本仓库。

## 动画

Codex 每个形态使用 8 列×11 行、单格 192×208 像素的 v2 图集。前 9 行对应待机、左右移动、挥手、跳跃、失败、等待、处理任务和检查；后 2 行是 16 个指针方向。未使用的格子保持透明，待机行的第 7 格是正面静止姿态。

帧通过原像素画的整体弹跳、局部位移、眼皮变化及头部轻微摆动构成。眨眼只改变虹膜附近的一小行，保留眼白；跳跃动作不重新上色眼睛。动作刻意维持像素网格，避免平滑插值。原画是静态正面画，侧面和背面信息缺失，因此指针方向只做轻度转向，不能等同于完整手绘转身动画。

## 桌面程序

Windows 版由 Tkinter 和 Pillow 实现。无边框置顶窗口以洋红色作为透明键，播放两套图集的动画格；30 帧左右的定时循环负责待机、散步和跳跃。双击有短暂防重复间隔，避免系统把连续点击识别为三击、四击时发生两次变身。右键菜单和拖动位置都只在本机生效。

## 验证与限制

两套 Codex 图集按 `spriteVersionNumber: 2` 打包，已通过 hatch-pet 的 `validate_atlas.py --require-v2` 结构与透明度检查。桌面版进行了启动、双向双击切换和大小调整的快速测试。方向语义的独立盲测尚未完成，因此不能保证所有 16 个方向在小尺寸下都清晰可辨。
